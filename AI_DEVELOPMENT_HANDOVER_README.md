# ⚠️ AI_DEVELOPMENT_HANDOVER_README ⚠️
> **[致下一个接手的 AI 助手]** 
> 这是一个专门写给你看的“上下文交接文档”（Context Handoff Document）。
> 请务必在开始任何新任务前仔细阅读本文档，以了解当前项目的架构状态、已完成的核心修改以及未来的开发方向，避免重复造轮子或破坏已有的稳定架构！

## 1. 项目全解 (Project Overview)
**项目名称**：Kumiko AI (Amadeus Program)
**运行环境**：基于 Electron + React（使用 Vite 构建）的桌面客户端。
**核心定位**：这是一个模拟真人聊天软件（类微信/Line）的沉浸式 AI 陪伴桌面应用，主角是《吹响吧！上低音号》中的黄前久美子（设定为 24 岁的教师）。
**核心机制**：
- **无上帝视角机制 (State Blindness)**：AI 仅仅是一个活在自己独立时间轴（JST，日本标准时间）中的真人，基于对等原则，**不能**感知用户是否在线、是否在使用电脑，也不能发送“快滚去睡觉”这种只有跨维度才能看到的越界消息。
- **主动发信（后台常驻心跳引擎）**：客户端依靠 Electron 的后台常驻能力，在前端 React (`App.tsx`) 中挂载了一个 `setInterval` 轮询来匹配日本时间的早、中、晚特定生活节点，并调用模型在脱离前台的情况下生成早安、晚安、随机闲聊等关怀推送。
- **真实的人际关系**：AI 的提示词和 RAG 数据库中引入了她已有的恋人（冢本秀一），AI 的聊天应该具备已确立稳固感情的成年女性的现实生活感，而非俗套的二次元讨好型擦边人设。

## 2. 之前的所有核心修改记录 (Changelog & Modifications)
以下是上一个 AI 已经完成的最核心的底层改造，请绝对不要轻易回滚更改：

1. **重构了主动消息架构 (Native Proactive Engine)**
   - 移除了旧版基于前端活跃度唤醒的“欢迎回来”这种具有上帝视角状态感知的蹩脚逻辑。
   - 引入了基于 `App.tsx` 连续心跳机制触发的主动关怀。生成后的内容通过底层的 `triggerNativeProactiveMessage` 通过 `ipcRenderer` 与主进程通信，抛出操作系统的原生通知（Windows Desktop Toast）。用户点击系统通知，即可通过 `ipcMain.on('show-window')` 把藏在托盘后台的软件拉回前台。
   - 在 `SettingsPanel.tsx`（通用设置段落）添加了名为“后台活动与推送唤醒”（`enable_proactive_messaging`）的 UI 图形开关，该开关通过读写 `localStorage` 直接控制前端轮询的放行与截断。
2. **彻底大修与重整了打包规范 (`package.json`)**
   - **修复了启动黑屏的致命 Bug**：修正了 Vite 的前端编译寻路路经。给 `npm run desktop:build` 执行段加入了 `vite build --base ./`。现在 `dist` 下的 `index.html` 会强行使用相对路径（`./assets/...`）寻址，再也不会因为 `file:///` 协议加载绝对路径报错而致使全端黑屏了。
   - **修正了 `nsis`（Windows 安装向导）参数**：
     - 去除了 `oneClick: true`，加入了经典向导与自选路径（`allowToChangeInstallationDirectory: true`）。
     - 开启了 `perMachine: true` 和 `allowElevation: true`，强制让向导触发操作系统的 UAC（管理员高级权限）确认窗，提升正规体验度。
     - 外部挂载了宏文件 `build/installer.nsh`，使用 `ShowInstDetails show` 将安装日志（比如解压 `app.asar` 的过程）直接展开在安装界面上，大幅增强了“正在安装大量文件”的极客可视反馈感。
3. **优化输出产物名称**
   - 在打包指令中挂载了完整的中文重命名，生成了：`Kumiko AI_完整安装包.exe` 和 `Kumiko AI_绿色免安装运行版.exe` 两个文件。并且将变量 `${version}` 去除，再也不会出现 `0.0.0` 这种不美观的名字。
4. **澄清过关于 `API Key` 防泄漏的误区（请谨记）**
   - **源码中绝对没有硬编码过用户的 API 密钥！** 当用户打包新 `.exe` 后安装并打开，却发现 API 已经被填好，这只是因为该 Electron 应用直接读取了同一台电脑中 `%AppData%/Roaming/[应用名]/Local Storage` 的缓存而已（即之前处于开发模式缓存的数据）。切记告诉用户，这绝对不是打在包里的，发给其他用户必定是完美清空白板！

## 3. 接下来可能要做的事 (TODO List for Next Steps)
你接手后，可能需要继续推进的工作涉及以下几点：

- [ ] **RAG 数据库扩充与 Prompt 精调**：继续细化黄前久美子作为社会职场人士的心态，完善与秀一的情感切片记忆库（核心位于 `constants.ts` 及其引用的底层 Prompt 中），防止角色降智。
- [ ] **沉浸感 UI/UX 改善**：可能还需要对面部立绘的高级动画过渡映射、仿原生 IM 软件（如微信 / LINE）的聊天气泡材质和主题配色再做打磨。
- [ ] **多模态与语音能力拓展**：如用户有进一步需求，可评估接入高质量的 TTS（语音合成 API 赋予其真人发音），以及基于 Electron `desktopCapturer` 赋予其“查看用户屏幕（截图）”的跨维度视觉能力。
- [ ] **托盘与窗口菜单优化**：Electron 主进程 (`electron-main.cjs`) 的系统 Tray 托盘还可继续增加“彻底退出心跳”、“清空今日对话”、“快捷调动休眠模式”等能快速向 React 渲染进程派发事件的快捷右键菜单项。

> **给继任者的完结语：** 
> 接手项目的老兄/姊妹，请根据上述全部上下文信息，顺畅且自信地接着往下编程！遇到没把握的核心变量，一定要先用 `grep_search` 等工具探明白再写。我相信你肯定能做得更好，绝不给咱们模型丢人！加油！
